const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGO_URI).then(() = console.log('SUCCESS'); process.exit(0); }).catch(err = console.error('ERROR:', err.message); process.exit(1); });  
